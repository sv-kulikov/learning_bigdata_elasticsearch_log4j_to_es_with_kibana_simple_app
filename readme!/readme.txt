manual.docx -- detailed description for this demo;
docker-compose.yml -- for starting all the necessary software;
logstash_es.conf -- the file that should be used as Logstash config (/usr/share/logstash/pipeline/logstash_es.conf, copy the file into container and start it again)